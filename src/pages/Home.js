import React from "react";
import LoginPage from "./LoginPage";

const Home = () => {
  return (
    <div className="flex flex-col h-screen">
      <LoginPage />
    </div>
  );
};

export default Home;
